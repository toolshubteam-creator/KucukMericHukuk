| Sayfa | Form | Perf | A11y | Best | SEO |
|---|---|---:|---:|---:|---:|
| about | desktop | **99** ✅ | **100** ✅ | **100** ✅ | **100** ✅ |
| about | mobile | 74 🟡 | **100** ✅ | **100** ✅ | **100** ✅ |
| article-detail | desktop | **99** ✅ | **96** ✅ | **100** ✅ | **100** ✅ |
| article-detail | mobile | 85 🟡 | **96** ✅ | **100** ✅ | **100** ✅ |
| contact | desktop | **95** ✅ | **93** ✅ | **100** ✅ | **100** ✅ |
| contact | mobile | 67 🔴 | **93** ✅ | **100** ✅ | **100** ✅ |
| home | desktop | **99** ✅ | **96** ✅ | **100** ✅ | **100** ✅ |
| home | mobile | **90** ✅ | **96** ✅ | **100** ✅ | **100** ✅ |
| service-detail | desktop | **99** ✅ | **96** ✅ | **100** ✅ | **100** ✅ |
| service-detail | mobile | **93** ✅ | **96** ✅ | **100** ✅ | **100** ✅ |

## Sayfa Bazlı Top Issue'lar

(Lighthouse audit score < 0.9 olanlar, ilk 8'i)

### about-desktop
- **0/100** Network dependency tree (`network-dependency-tree-insight`)
- **0/100** Render-blocking requests (`render-blocking-insight`) — *Est savings of 470 ms*
- **50/100** Reduce unused CSS (`unused-css-rules`) — *Est savings of 13 KiB*
- **50/100** Use efficient cache lifetimes (`cache-insight`) — *Est savings of 34 KiB*
- **50/100** Document request latency (`document-latency-insight`) — *Est savings of 5 KiB*

### about-mobile
- **0/100** Reduce unused CSS (`unused-css-rules`) — *Est savings of 21 KiB*
- **0/100** Network dependency tree (`network-dependency-tree-insight`)
- **0/100** Render-blocking requests (`render-blocking-insight`) — *Est savings of 3,400 ms*
- **17/100** First Contentful Paint (`first-contentful-paint`) — *4.3 s*
- **41/100** Largest Contentful Paint (`largest-contentful-paint`) — *4.3 s*
- **50/100** Use efficient cache lifetimes (`cache-insight`) — *Est savings of 34 KiB*
- **50/100** Document request latency (`document-latency-insight`) — *Est savings of 5 KiB*
- **75/100** Speed Index (`speed-index`) — *4.3 s*

### article-detail-desktop
- **0/100** Background and foreground colors do not have a sufficient contrast ratio. (`color-contrast`)
- **0/100** Network dependency tree (`network-dependency-tree-insight`)
- **0/100** Render-blocking requests (`render-blocking-insight`) — *Est savings of 430 ms*
- **50/100** Use efficient cache lifetimes (`cache-insight`) — *Est savings of 34 KiB*
- **50/100** Document request latency (`document-latency-insight`) — *Est savings of 8 KiB*

### article-detail-mobile
- **0/100** Minimize main-thread work (`mainthread-work-breakdown`) — *2.2 s*
- **0/100** Background and foreground colors do not have a sufficient contrast ratio. (`color-contrast`)
- **0/100** Forced reflow (`forced-reflow-insight`)
- **0/100** Network dependency tree (`network-dependency-tree-insight`)
- **0/100** Render-blocking requests (`render-blocking-insight`) — *Est savings of 1,360 ms*
- **50/100** Reduce unused CSS (`unused-css-rules`) — *Est savings of 17 KiB*
- **50/100** Use efficient cache lifetimes (`cache-insight`) — *Est savings of 34 KiB*
- **50/100** Document request latency (`document-latency-insight`) — *Est savings of 8 KiB*

### contact-desktop
- **0/100** Background and foreground colors do not have a sufficient contrast ratio. (`color-contrast`)
- **0/100** Links rely on color to be distinguishable. (`link-in-text-block`)
- **0/100** Reduce unused CSS (`unused-css-rules`) — *Est savings of 22 KiB*
- **0/100** Network dependency tree (`network-dependency-tree-insight`)
- **0/100** Render-blocking requests (`render-blocking-insight`) — *Est savings of 550 ms*
- **50/100** Use efficient cache lifetimes (`cache-insight`) — *Est savings of 34 KiB*
- **50/100** Document request latency (`document-latency-insight`) — *Est savings of 9 KiB*
- **79/100** First Contentful Paint (`first-contentful-paint`) — *1.1 s*

### contact-mobile
- **0/100** Background and foreground colors do not have a sufficient contrast ratio. (`color-contrast`)
- **0/100** Links rely on color to be distinguishable. (`link-in-text-block`)
- **0/100** Reduce unused CSS (`unused-css-rules`) — *Est savings of 18 KiB*
- **0/100** Network dependency tree (`network-dependency-tree-insight`)
- **0/100** Render-blocking requests (`render-blocking-insight`) — *Est savings of 2,550 ms*
- **13/100** First Contentful Paint (`first-contentful-paint`) — *4.6 s*
- **23/100** Largest Contentful Paint (`largest-contentful-paint`) — *5.2 s*
- **50/100** Use efficient cache lifetimes (`cache-insight`) — *Est savings of 34 KiB*

### home-desktop
- **0/100** Background and foreground colors do not have a sufficient contrast ratio. (`color-contrast`)
- **0/100** Reduce unused CSS (`unused-css-rules`) — *Est savings of 15 KiB*
- **0/100** Network dependency tree (`network-dependency-tree-insight`)
- **0/100** Render-blocking requests (`render-blocking-insight`) — *Est savings of 380 ms*
- **50/100** Use efficient cache lifetimes (`cache-insight`) — *Est savings of 34 KiB*
- **50/100** Document request latency (`document-latency-insight`) — *Est savings of 12 KiB*

### home-mobile
- **0/100** Minimize main-thread work (`mainthread-work-breakdown`) — *2.2 s*
- **0/100** Background and foreground colors do not have a sufficient contrast ratio. (`color-contrast`)
- **0/100** Network dependency tree (`network-dependency-tree-insight`)
- **0/100** Render-blocking requests (`render-blocking-insight`) — *Est savings of 1,140 ms*
- **50/100** Use efficient cache lifetimes (`cache-insight`) — *Est savings of 34 KiB*
- **50/100** Document request latency (`document-latency-insight`) — *Est savings of 12 KiB*
- **62/100** First Contentful Paint (`first-contentful-paint`) — *2.6 s*
- **86/100** Time to Interactive (`interactive`) — *4.2 s*

### service-detail-desktop
- **0/100** Background and foreground colors do not have a sufficient contrast ratio. (`color-contrast`)
- **0/100** Network dependency tree (`network-dependency-tree-insight`)
- **0/100** Render-blocking requests (`render-blocking-insight`) — *Est savings of 430 ms*
- **50/100** Reduce unused CSS (`unused-css-rules`) — *Est savings of 16 KiB*
- **50/100** Use efficient cache lifetimes (`cache-insight`) — *Est savings of 34 KiB*
- **50/100** Document request latency (`document-latency-insight`) — *Est savings of 7 KiB*

### service-detail-mobile
- **0/100** Background and foreground colors do not have a sufficient contrast ratio. (`color-contrast`)
- **0/100** Network dependency tree (`network-dependency-tree-insight`)
- **0/100** Render-blocking requests (`render-blocking-insight`) — *Est savings of 1,410 ms*
- **50/100** Reduce unused CSS (`unused-css-rules`) — *Est savings of 24 KiB*
- **50/100** Use efficient cache lifetimes (`cache-insight`) — *Est savings of 34 KiB*
- **50/100** Document request latency (`document-latency-insight`) — *Est savings of 7 KiB*
- **64/100** First Contentful Paint (`first-contentful-paint`) — *2.6 s*
- **87/100** Time to Interactive (`interactive`) — *4.1 s*

